# 开发环境配置
server:
  host: 0.0.0.0
  port: 8080

db:
  host: localhost
  port: 3306
  user: root
  password: 123456
  database: blog
  charset: utf8mb4

redis:
  host: localhost
  port: 6379
  db: 0

jwt:
  secret: dev-secret-key
  expire_hour: 24

app:
  upload_path: uploads
  max_upload_size: 10

rate_limit:
  enabled: true
  requests: 1000
  window_sec: 60
